# Suri — regression checklist

This project has no test suite and no CI gate. `main` deploys straight to production, to a Capacitor
shell and a PWA that children open directly — there is no staging ring, no phased rollout, and no
one between a merge and a kid's phone. This document is the manual gate until an automated one
exists. Run it against the dev server (`npm run dev`, default `http://localhost:5180`) before any
merge to `main` touches rendering, layout, or a game's state machine.

## What a release must satisfy

- **No console errors** on load or during play, on any of the five reference sizes below, for any
  of the twelve pages.
- **Every game reaches its own "done" screen** at least once per release (won/lost/finished/museum
  — see the per-game table). A game that cannot be completed is a release blocker, not a bug ticket.
- **No control is unreachable or overlapped.** Every button a child needs is fully on screen, not
  hidden under the Suri home link, and at least 44×44 px.
- **No stale frame.** Backgrounds and boards repaint when the thing they depict changes, even if the
  canvas pixel size does not (see "cached layers" below).
- **The hub's cards all resolve** to a working game page and back again.
- Session boundaries hold: nothing loops forever, nothing nags to return. If a change touches a
  results/complete screen, confirm it still ends the session rather than auto-advancing.

If any of the above fails, the build does not go out, regardless of schedule pressure. Escalate to
producer, don't ship around it.

## Per-game critical path

Each path is written to run on a phone in under a minute. Where a debug handle exists, open it
from a laptop devtools console tethered to the phone (or just Chrome devtools on desktop at a
narrow viewport) and read it after the step described — the point is to confirm state, not to
replace playing the game with your thumb.

Status note: the mill, tidepool, market, dig and puffball loops below were played today at four
sizes and passed. Nachtwacht was played through at 320×568, 390×844 and 844×390 today, swipe and
all. Cloudhopper, Planetarium, and the hub critical paths below are written from
reading the code, not from having played them this session — treat them as **unverified** until
someone actually runs them.

| Game | Path (what to do) | What the debug handle should report |
|---|---|---|
| **Hub** (`index.html`) *(unverified)* | Load the page. Confirm 11 game cards render with thumbnails/photos, not blank tiles. Tap "Cloudhopper" (top-left card). Confirm it navigates to `cloudhopper.html` and the game loads. Tap the Suri link top-right inside that game. Confirm you land back on the hub. | No debug handle on the hub. Check visually: `document.querySelectorAll('.gamecard').length === 11` in devtools, and that none of the 11 `href`s 404. |
| **Cloudhopper** (`cloudhopper.html`) *(unverified)* | From the hub, open Cloudhopper. The title screen offers one road, "The journey" — check there is no second mode button beside it and no shop icon in the top bar. Open it: islands and real airports should be numbered 1, 2, 3 … in one grid, in the order the stars open them, with "You are here" on the first unfinished open stop. Tap stop 1, Mission 1 → play. Draw a route from the first aircraft to the runway and let it land. While it is still rolling out, route a second aircraft to the same runway: once the first is about a quarter of the way down the strip, the second should be cleared in rather than sent around. Then tap the green build button under the pause button: the upgrade list should open over the paused airport, a bought upgrade should apply at once (buy Radar and the distance readout should start appearing further out), and the back arrow should return you to the same shift. The mission-complete card carries the same list on its gold Upgrades button. | `window.__wh.getMode()` should read `'playing'` once the mission starts. After one landing, `window.__wh.getWorld().planes.length` should have dropped by one and `.failure` should still be `null`/undefined (no crash was recorded). |
| **Nachtwacht** (`nightwatch.html`) | Open the game on a save that has never played it. The first figure must be **De Vlieger**, four stars in a diamond, and the line under the heading must read *Veeg in één beweging van ster naar ster*. Let it show and wait for the lines to go out. Now draw it back **without lifting your finger**: press the top star and run the finger round all four and back to the top. Each star must light and click as the finger reaches it, a ring must fly off it, the phone must tick, and the note must climb a step per star - and the dashed line must sit on the fingertip every frame, never trailing it. Before you finish, run the finger back over the star before last: that line must come off again. Then finish it. The card must name it and give its bit of lore. Now do the next one the old way instead - tap one star, lift, tap the next - and check that nothing is lost by lifting. Keep going. By the fifth or sixth figure the sky should turn over once the lines are out, with *De hemel draait* over it, and once the eight hand-drawn ones run out - sooner if you are quick, because a clean run skips a rung - the figures should stop having names and start being labelled *9 sterren, 8 lijnen*. Every six brings up **Even uitrusten** listing what you brought back, with **Nog een** under it and a back arrow beside the way home; nothing else in the game shows that arrow. Get one deliberately wrong twice in a row: the second time the sky must show you what it was rather than making you keep trying, and the next figure must be gentler. Turn the phone sideways mid-game: the heading, the dots and the button must all stay clear of the sky and of the two round buttons, and no two stars may ever end up close enough that you cannot tell which one your thumb is on. | `window.__nw.debugState()`: `phase` goes `showing` → (`turning`) → `drawing` → `solved`/`wrong`/`missed`, and `rest` every sixth figure. `difficulty` and `move` are what the platform decided - `move` should read `up` or `stretch` after a clean run and `bridge` after two wrong in a row, with `difficulty` falling. `figure` is the figure's id, `made` whether it is hand-drawn, `oneStroke` whether it can be drawn without lifting. `stars` gives every star in **client coordinates** with `fig` saying which are the figure's, so a test can aim at them directly; `edges` is the figure in one-stroke order, `drawn` what is on the sky and `need` how many lines it wants. `hand` is the run of stars under the finger right now and `anchor` the star a lifted finger left waiting. `closest` must never fall below `minGap`, and `minGap` must always exceed `grab`. `skills` shows visual memory and pattern, which are also what is written to `save.skills`. |
| **Planetarium** (`orbit.html`) *(unverified)* | Open the game (it opens on the Puzzle tab). Tap the planets in order, nearest the sun first, for round 1 (4 rocky planets). Then tap the Explore, To scale, and Missions tabs once each just to confirm they render without a crash. | `window.__orbit.debugState()`: `phase` starts `'picking'`, `placed` increments with each correct tap, `next` shows the id of the planet still wanted. After the 4th correct tap, `phase` should move on (`roundDone` or the next round starts and `round` becomes `1`). |
| **Opgraving** (`dig.html`) | Open the game, pick the first site. Use the tools to expose bone, then answer the species question, then let it reveal. | `window.__dig.debugState()`: `phase` goes `dig` → `ask` → `reveal` (or `wrong` if the guess is wrong), `exposed` rises above 0 as you brush/chisel, and `found` increases by one once the specimen is confirmed in the museum. |
| **Stuifzwam** (`puffball.html`) | Open the game, pick night 1. Tap an arrow once: the hedgehog should move exactly one square and stop. Hold it: it should keep walking and stop on the next square when you let go. Then drop a puffball, count the lit squares, move away, let it pop, catch a mole or clear a pot. | `window.__puff.debugState()`: `phase` is `play`, `puffs` goes from 0 to 1 while it's ticking and back to 0 after it pops, `reach` matches the number printed on the cap, and either `knocks` increases (a mole caught) or `potsLeft` decreases. |
| **Moonshot** (`moonshot.html`) | Open the game; the starter rocket is on the pad. The shelf has five tabs and pages within them; step through every page of every tab and check nothing is a blank square. Tap an engine on the rocket: a panel should offer ignition delays, and picking one should put a yellow badge on that engine. Tap a cargo bay on the rocket: the hold should open with two slots, tapping Rover and Camera should fill it, the bay should show two gold dots, and the pill under the destination chip should read "photo · hold 2/2". Fly a rocket carrying a camera, a solar panel, an aerial, a parachute, a leg and a flag: the card at the end must list a picture coming home, a landing under the parachute, and the flag planted - none of that kit may fall away with the first stage. Tap the destination chip top-left: the whole list of places should appear with NASA photographs and the speed each one needs. Pick Mars. Tap the Motoren tab and tap the big engine (it should go *under* the stack, not on top). Tap Tanks and tap a tank (it should slide in *under* the capsule). Tap Boosters and tap a booster twice (one should land on each side). Drag one booster down onto the shelf to bin it. Press Undo. Then Launch, steer with the two thumb pads, and let the fuel run out. On the way up watch the rail down the right-hand edge: the marks you pass (birds, aeroplanes, the edge of the air) should turn gold, the dashed gold line should sit at your best height, and going over it should jolt the frame and say so. Birds should pass in the first kilometre, a weather balloon around thirty, satellites from four hundred; when the engines stop, the view should keep climbing with the number rather than freezing. Turn the phone sideways mid-flight: the rail moves to the left edge and must not sit under the stage button or the thumb pads. | `window.__moon.debugState()`: `phase` goes `build` → `count` → `fly` → `coast` → `done`. In `build`, `design` lists every part as `id@col,row`, `problem` is the one line shown on screen (null when nothing is wrong), and `stages` shows how many stages each column has. During `fly`, `altKm` and `vUp` rise and `dropped` climbs each time a stage falls away — side boosters should drop before the core. At `done`, `topKm` is the height reached, `best` the highest rung recorded, and the card should show where you got to with its photograph plus how much more speed your chosen target wanted. Build something deliberately wrong and check `problem` names it: a tank in a column of its own (dead weight), two engines stacked (never lights), one booster on one side only (lopsided), no capsule at all. |
| **Watermolen** (`mill.html`) | Open the game, pick valley 1. It should stay dry: a card says what the valley wants ("Breng het water naar 1 akker"), a countdown button says how long you have, and the card fades after about six seconds. Dig a channel from spring to field, then press the button rather than waiting it out. | `window.__mill.debugState()`: `prep` counts down from 15 and `water` stays 0 until it reaches 0 or the button is pressed. `phase` moves `levels` → `play` → `won` (or `failed` if the channel floods/stalls), `water` (total water) rises above 0, and `wasted` should not run away while you're actively directing flow. |
| **Getijdenpoel** (`tidepool.html`) | Open the game, pick tide 1. Sort three or four creatures by the shown rule, then let the rule change once and sort one more correctly. | `window.__tide.debugState()`: `phase` is `play`, `rule` changes value partway through the level, `correct` increments on right sorts, `wrong`/`missed` should not climb on the ones you got right, and `resolved` tracks total handled. |
| **Stroomkring** (`circuit.html`) | Open the game. It must open on puzzle 1 with a battery and a bulb already on the bench and a line under the board reading *de batterij zit nog nergens aan vast*, with a ring round the battery. Press a finger on an empty cell beside the battery and draw: a line of copper wire must follow the finger one square at a time, and the line under the board must change to *hier zit een gat in de kring* and ring the loose end. Close the loop: the bulb lights, beads of current run round the wire the way the current really goes, the reading strip says about 0,36 A, and the card with the star comes up with **Volgende** and **Doorspelen** - neither may cover the circuit you have just built. Now draw a second line straight across the battery: the wire must glow, the reading must jump to about 8 A in orange, the bulb must go out and the line must read *dit is kortsluiting*. Watch the **over** bar empty in a few seconds and the line change to *de batterij is leeg*, then press **Nieuwe batterij**. Go to the list with the chip top left and pick puzzle 4: two bulbs in parallel, each at nine tenths, and opening one branch's switch must leave the other lit. Pick puzzle 9 and check the relay: with the little loop closed, the bulb in the big loop lights; tap a wire in the little loop to take it out and the bulb must go dark. Then the bench (last in the list): fourteen parts on the shelf over two pages, split evenly over the two and centred on both, with *krokodillenklem* broken over two lines rather than squashed into one. Whatever you build on the bench must still be there after a reload, and a save edited by hand (`circuit.solved` set to a string, `circuit.bench` to rubbish) must open the bench empty rather than broken, with the chip reading 0/9. Check the list itself: it covers the bench outright with nothing ghosting through, every one of the ten is openable, each has a star - an empty socket until it is earned - and the back button is hidden while the list is up and back when it is closed. Turn the phone sideways on any of them: the shelf must move to a column down the right, the star card must lie along the foot of the screen clear of the pegboard and of the line under it, and nothing - not the goal card, not the shelf - may sit under the two round buttons top right. Upright, the goal card must start below those buttons, not under them. | `window.__circuit.debugState()`: `puzzle` and `puzzleId` say which one is open and `goal` what it asks; `board` lists every part with its cell, its quarter turn and, for a wire, the mask of the faces the line was drawn through; `fault` is `null` or `<kind>@<index>` and `faultLine` the sentence for it; `amps` is what the sources are giving out and `duty` what each part is doing, where a bulb at 1 is at full power; `short` says whether the current is running away; `charge` is what is left in each battery as a percentage; `marks` is everything the board has been seen to do and `needs` what this puzzle wants, and `done` when they meet; `grid` gives `ox`, `oy` and `unit`, so a cell can be turned into a point and the whole game played with real pointer drags; `buttons` lists every drawn control with its centre. |
| **Dierenboek** (`animals.html`) | Open the page. It should show a loading ring of drawn animals for a moment and then nine coloured shelf tiles, each with a drawn animal and a count, under a line saying how many of the animals you have looked at. Tap **Zoogdieren**: a grid of photograph cards, two or three to a row, names underneath. Photographs arrive over the network, so watch a card go from a shimmering coloured panel to a real animal - if a card is still a faint silhouette after a few seconds, that photograph failed and the fallback is what you are looking at. Scroll well down the grid and back: cards must keep filling in and nothing may flicker. Tap a card, then come back with the chevron: that card must now be green - card, edge and name - with a filled green tick in the corner, and the shelf tile behind it must count it (*1 van de 439 bekeken*). Tap a card. The animal's page must show a large photograph with the photographer and licence along its foot, the name in Dutch, English and Latin, its length written large under *Hoe groot* (with *gemiddeld voor zijn familie* where the length is its family's), and no ruler or silhouette under it, a world map with the right continents lit, the facts, and the Wikipedia paragraph. Press **Volgende** a few times. Go back twice, tap **Zoeken**, type `l e e u`: results should appear from the second or third letter. Tap one, then **Verras me** - it must open an animal you have not seen. Go back to the shelves: the count and the green bar must have moved up. | `window.__animals.debugState()`: `view` goes `loading` → `shelves` → `grid` → `detail` → `search`, `count` is the whole book (about two thousand), `shown` is the shelf or the search results, `open` is the scientific name of the animal on screen, `seen` climbs by one per new animal and never falls, and `buttons` lists every hit box currently on screen with its centre. `window.__animals.tap('shelf:bir')` presses a button by name without aiming, and `window.__animals.scrollTo(px)` puts a long list where you want it. |
| **Marktdag** (`market.html`) | Open the game, pick market day 1. Fill a customer's basket to match their pictured/numeral order and serve them; repeat for a second customer. | `window.__market.debugState()`: `phase` is `play`, `served` increments per correct order, `baskets` reflects what's currently in the stall, `patience` counts down for the active customer and resets/advances on serve, `wrong` should not climb on correct fills. |
| **Klokkijken** (`clock.html`) | Open the game; nine level cards, each with a real clock face on it. Pick level 1 and read three whole hours. Get one deliberately wrong: the ring round the clock goes red, the hour hand lights up orange, the slice of dial it is inside is shaded, the number it has left is circled, the card under the question reads the time out in Dutch ("half vier · 3:30"), the right answer goes green and the one you picked goes red, and a Verder button carries on - it must never dead-end and never fail you out. Finish the level and check the stars. Then pick level 5 (Zet de klok) and drag the hands: dragging the long hand must carry the short hand along with it, and taking the long hand past twelve must move the hour on by one. Take the long hand backwards past twelve and the hour must go back. Press the 5 10 button top left: the minute ring appears on the dial and survives a reload. Check level 7 (Zoek de klok) and level 9 (Hoeveel later) render and can be answered, in portrait and turned sideways - on its side the four faces move to the left half and the digital clock to the right. | `window.__clock.debugState()`: `phase` goes `levels` → `play` → `won`; `kind` is one of `read`/`set`/`match`/`elapsed`; `target` is the time asked about and `hands` the hands as they stand; `spokenNl` must read "half vier" for 3:30 and "tien voor half vier" for 3:20; `feedback` goes `none` → `right`/`wrong` → `none`; `firstTry` counts only answers given before the clock explained itself, and `stars` follows from it. `buttons` lists every drawn control, and after a wrong answer must include `go`. |

| **Klankhuis** (`rhythm.html`) | Open the page. It must open on the instrument, not a menu: nine coloured chime bars along the bottom, long on the left and short on the right, with a hand pointing at one. Hit every bar in turn - each must sound a different pitch, rising left to right, with a strike you can hear and a ring that lasts about a second. Tap several at once: it must not clip or buzz. Tap **Niveaus**, then level 1. A bar of clicks counts you in with a big 4, 3, 2, 1, then a ball falls and bounces on the line on every beat. Tap the drum (or anywhere) on each bounce: a tight green ring lands on the mark. Now deliberately tap well after a bounce: a wide red ring lands where you tapped with a dashed line back to the beat it belonged to, the card says you were late, and then that one bar plays again at half speed with the strip round it lit. Finish the level and check the stars count only the bars you got right first time. Open level 5 (Echo): the game plays three chimes, which light up as they sound, then you play them back. Get one wrong on purpose - the right note must sound about a third of a second later, the bar you hit gets a red cross, the right bar a dashed ring, and the game carries on rather than stopping. **Nog eens** must replay the phrase and cost nothing. Open level 9 (Zelf maken) - it must be open even on a fresh save, and reachable straight from the opening screen. Tap eight or ten squares, press **Spelen**: the loop runs, the column it is on lights up, and the notes sound. Press the instrument button through all four voices and the tempo button through all four speeds. Reload the page and open Zelf maken again: the same squares, the same instrument, the same tempo. | `window.__rhythm.debugState()`: `phase` goes `home` → `levels` → `play` → `won`; `step` goes `count` → `run` → `judge` → `teach` in the tapping levels and `listen` → `answer` → `judge` in the ear levels. The timing is provable rather than a matter of opinion: for every entry in `scheduled`, `at` must equal `t0 + beat * beatSeconds` to within a rounding error, and `nextAskAt` is the exact moment on the audio clock of the beat still to be hit - tap at `nextAskAt + outputLatency` and the last entry of `taps` must read `grade: 'perfect'` with `deltaMs` inside 60. `got` holds one grade per asked beat and `clean` says whether the round was right first time. `grid` is the sequencer as eight numbers, one per step with a bit per chime, and must round-trip through `localStorage` unchanged; a save with rubbish in it must come back as eight zeroes rather than throw. |

| **Rekenrijk** (`numbers.html`) | Open the game; nine level cards, each with a drawn picture of the objects that level uses. Pick level 1 (Splitsen tot 10). The answer buttons must NOT be there yet - a panel where they will be says what to do with the things first. Drag the green divider along the bead rack: the beads run into the two bowls underneath, both numbers change as you drag, and the written sum on the card fills itself in (`10 = 4 + □`). When the left bowl holds the number asked for, the four answers appear. Answer one deliberately wrong: the right answer goes green, the one you picked goes red, the beads in the right-hand bowl count themselves out one at a time, the card says the sum in Dutch words ("tien is vier en zes"), the strip underneath explains the step, and a **Verder** button carries on - it must never dead-end and never fail you out. Finish the level and check the stars. Then level 4 (De tien vol): drag loose apples into the ten-frame one at a time; the frame must go green and say "tien, en" at exactly ten, the first line of the sum (`8 + 2 = 10`) must fill in at that moment and not before, and the leftovers must be counted on from eleven. Level 6 (Over het tiental): drag the peg to the next ten - the answers appear only once it is there, and the jump `+3` is drawn on the line. Check that 27 + 8 offers 25 and 215 among the wrong answers. Level 8 (Keersommen): drag the corner handle of the crate to make the rows; the row totals down the right-hand side must read 9, 18, 27. Turn the phone sideways on any level: the objects move to the left half and the card and answers stack down the right. | `window.__numbers.debugState()`: `phase` goes `levels` → `play` → `won`; `inRule` must be `true` on every question of every level; `sum` and `wordsNl` are the sum written and said; `ready` is false until the objects have been arranged and no answer can be given before it turns true; `objects` holds the stage as numbers (`split`, `merge`, `taken`, `inFrame`, `placed`, `at`, `rows`, `cols`) and `needs` is what it has to reach; `feedback` goes `none` → `right`/`wrong` → `none`; `firstTry` counts only answers given before the objects showed the answer, and `stars` follows from it. `buttons` lists every drawn control and after a wrong answer must include `go`; `grabs` lists every object on the table that can be picked up, with its centre. |
| **Wereldatlas** (`atlas.html`) | Open the game; nine level cards, each with a little drawn map of the board it is played on. Pick level 1 (de provincies). It must open on the look round, not the puzzle: the map is the satellite photograph with the North Sea glittering blue, every province is tinted and named on it, Groningen is lit amber, the card underneath gives its fact, and the voice says the name and the fact. Tap Friesland on the map: it must light up and be told about; **Volgende** and **<** walk through all twelve. Press **Puzzelen**. One province sits in the tray at the foot of the screen and its name is on the card at the top; the map above shows the country with the empty places outlined. Drag the piece onto the map: the moment it leaves the tray it must grow to the size of the hole it is going into, and dropping it in the right place must snap it home with a click, its name written across it. Now drop one deliberately on the wrong province: it must spring back to the tray, the right place must light up amber with its name on it, the wrong one must get a red dashed outline, and the card must read *dat is Drenthe, Friesland ligt hier* with a fact underneath and a Verder button - it must never dead-end and never fail you out, and after Verder the piece must fly home by itself. Finish the twelve and check the stars only count what went home first time. Press **Volgende** into level 2 (het water) and press the third button in the top bar, the drawn dyke with the sea against it: the country must be redrawn by height and about a quarter of it must go blue. Press it again and the surge must rise to five metres and take the west; press it once more to put the dykes back. Place the Rijn and the Waal: dropping the Waal on the Rijn must be marked wrong and name the Rijn. On level 5 (Europa) or 7 (werelddelen), place one piece correctly and check the green card offers a **Dieren** button - pressing it must open `animals.html` with the shelves replaced by that part of the world (`animals.html#af` is Afrika), with the continent's name as the title. Press the dashed-square button top left: the outlines on the empty places must go away and survive a reload - and on levels 2 (het water), 6 (hoofdsteden) and 9 (vlaggen) that button must not be there at all, because on those the places a piece can go are already drawn. On level 8 (de landen van de wereld) every country handed over must be one a finger can land on: Rusland, Brazilië, India, Australië and their like, never Kenia or Nederland, which are on the map as facts but never dealt. On level 9, drag through to the Swiss flag: it must be a square with a white cross in the middle whose arms stop short of the edges, not a Nordic cross off to the left like Denemarken's and Zweden's. Turn the phone sideways on any level: the map must move to the left half and the card, the tray and whatever the game has to say must stack down the right. | `window.__atlas.debugState()`: `phase` goes `levels` → `explore` → `play` → `won`, and `explore` is the id being told about in the look round, whose buttons are `explore:prev`, `explore:next` and `explore:play`; `satellite` is true once the photograph is under the map; in the puzzle `hint` lights a region and says the fact, and `explore:again` goes back to looking; `piece` is the id in the tray and `pieceName` its name; `handAt` is where a drag must start and `homeAt` where a correct drag must end, so a whole level can be played with real pointer drags between the two; `targets` lists every place this level deals in with its centre on screen; `placed` grows by one per piece and `remaining` falls; `feedback` goes `none` → `right`/`wrong` → `none`; `firstTry` counts only pieces that went home before the map showed you, and `stars` follows from it; `sea` is `null` with the dykes on, `0` with them off and `5` in the storm; `outlines` is the switch. `buttons` lists every drawn control and after a wrong drop must include `go`. |
| **Letterbos** (`letters.html`) | Open the game; nine level cards, each with a picture of a word that level is about - a real photograph on most of them, and *Echte foto's van Wikimedia Commons* along the foot of the screen. Pick level 1 (Klanken). A picture fills the card at the top, three empty boxes sit under it and the letters lie on a rack at the foot of the screen - the word is never written anywhere. Words like *rat*, *maan*, *koe* and *krab* must show a real photograph filling the frame, cropped, with *Foto: <naam> · <licentie>* in a small dark strip along its bottom edge; words like *pen*, *bus*, *zes* and *fout* must keep their drawing and show no credit at all - never a blank and never a question mark. Tap the picture: the word is said out loud. Tap **Klank voor klank**: the same word in pieces. Drag a letter into a box: it lands with a wooden knock and says its own sound, and tapping a box that is already filled says that sound again. Finish the word: it is read back whole, the picture bounces, and the next one comes. Now drag a letter into the wrong box on purpose - it must spring back, the box it really belongs in must glow, the strip under the picture must say which sound goes there (*Hier hoort de p.*) and the sound must be spoken again slowly. Do it twice on the same word and the game must put the right tile in itself and read the word out - it must never dead-end and never fail you out. Play the six words out and check the stars only count the words built without help. Then level 2 (Lange klank): the rack must offer the `aa` as one tile **and** a single `a` beside it, and the two-letter tile must carry a tie under the letters. Level 4 (ei of ij): the word is already there except for one place; get it wrong and the word must be written out underneath with the right spelling in red and named (*de lange ij*). Level 7 (Woorden hakken): the word is written out whole and tapping between two letters cuts it - a cut in the middle of `aa` must be refused with *aa hoort bij elkaar*. Level 9 (Zinnetjes): four word tiles and a drawn scene, and the finished sentence reads itself. Press the **abc** button top left: the word appears above the boxes and the setting must survive a reload. Turn the phone sideways: the picture moves to the left half and the boxes and the rack stack down the right. On a machine with no Dutch voice nothing may hang - every level must still be playable in silence. Now turn the network off entirely and reload: every word must fall back to its drawing, the credit strips and the Wikimedia line must be gone, and all nine levels must play through exactly as before. Turn it back on and the photographs must fade in over the drawings, never over an empty frame. | `window.__letters.debugState()`: `phase` goes `levels` → `play` → `won`; `kind` is one of `build`/`choose`/`chop`/`ladder`/`sentence`; `word` and `parts` are the word and its sounds, `filled` fills up as tiles land and `solved` turns true when it matches; `cuts` and `gaps` are the chopping level; `wrongHere` counts the mistakes on this word and resets to 0 the moment the game helps; `firstTry` counts only words built with no help at all, and `stars` follows from it; `voice` says whether a Dutch voice was found, and the game must play identically when it is false. `photos` is `ready` or `failed` for the data file, `photosHeld` is how many words got a photograph (67), and `art` is `photo` or `drawn` for the word on screen with `credit` and `licence` beside it - `art` must never be `photo` with an empty `credit`. `slots` and `tiles` carry the centre of every box and every tile, so a whole level can be played with real pointer drags, and `buttons` lists every other drawn control. |

**Every game, every time:** the top right corner must show the same round house button, and, once you are inside a level, the same
round chevron beside it. No words in either. The chevron must be absent on a level list and present during play, and one tap must
come back out of the level without leaving the game.

For every game with a `hits`-bearing `debugState()` (mill, tidepool, market, dig, puffball, and
Planetarium's mission screen), `buttons` gives the on-screen centre of every tappable control — useful for
confirming a button that looks fine is actually where the game thinks it is, if a bug report says a
tap "does nothing" at a particular size.

## Checks that run without a browser

`npm test` bundles the rule modules with esbuild and asserts against them: when a runway is clear
for the next arrival, how a Moonshot design cuts into stages, what is dead weight, what the air
makes of a shape, who carries on walking in Stuifzwam, and that the journey lists every island and
every airport once, in a never-decreasing order of stars. It runs in about a second and needs no
device. Run it before every push; it is not a substitute for the paths below, but every rule it
covers is one that used to be checked only by eye.

## Cross-cutting checks

Run these on every page, not just the ones you changed — a shared CSS or util file (`uiScale`,
`style.css`, `render/look.ts`) can regress every game at once.

| Check | What to do | What "pass" looks like |
|---|---|---|
| Console errors | Open devtools console, load each of the 12 pages fresh | No red errors on load or during a short play session |
| Reference sizes | Resize/rotate to 390×844, 360×640, 844×390, 820×1180, 1440×900 | Layout adapts at all five; nothing requires a sixth to look right |
| Controls on screen | At each size, check every button/tool/HUD element | Nothing is clipped, pushed off-canvas, or requires scrolling to reach (games don't scroll; the hub does) |
| Suri link collisions | At each size, check the top-right home link against nearby HUD elements | The link never overlaps a game button; nothing sits underneath it un-tappably |
| Touch target size | Measure any control that looks tight, especially in landscape | Every tappable control is at least 44×44 px |
| Orientation flip mid-session | Rotate the device while a level is in progress, not just before starting one | The layout re-flows without freezing the game or losing state (`debugState()` still responds) |
| Back link round-trip | From inside each game, tap the Suri link | Returns to `index.html`, hub renders normally, no stuck overlay |

## What has broken before — re-check every time

1. **A canvas not sized by CSS because its id is missing from the fullscreen rule.** The rule in
   `src/style.css` is:
   `#game, #sky, #space, #dig, #valley, #pool, #stall, #wood { position: fixed; inset: 0; width: 100%; height: 100%; ... }`
   These eight ids must cover all eight game canvases (`game` = Cloudhopper, `sky` = Nachtwacht,
   `space` = Planetarium, `dig` = Opgraving, `valley` = Mill, `pool` = Getijdenpoel, `stall` = Market,
   `wood` = Stuifzwam). This is what broke Stuifzwam before — its canvas id (`wood`) had been left out
   of the rule, so the canvas rendered at its default intrinsic size instead of filling the screen.
   **Re-check:** whenever `style.css` changes, or a game's HTML file changes, confirm its canvas id
   is still in that selector list, and that the canvas actually fills the viewport on load.

2. **Layout that assumes a tall screen and collapses in landscape.** This broke Marktdag,
   Getijdenpoel, and Opgraving previously — UI built for portrait aspect ratios (stall counter, pool
   rows, dig site framing) either overlapped itself or ran off-screen once width exceeded height.
   **Re-check:** every game, not just these three, at 844×390 (landscape phone) specifically, not
   just at the tall sizes. Test starting a level already in landscape, and also rotating into
   landscape mid-level.

3. **A cached drawing layer keyed on too little, so it shows a stale picture.** `render/look.ts`'s
   `CachedLayer.get(w, h, key, draw)` only redraws when the canvas size *or* the caller's `key`
   string changes. Several games pass a `key` that does not include everything that affects the
   picture — for example Nachtwacht's sky layer uses the constant key `'night'` and Getijdenpoel's
   sand layer uses the constant key `'sand'`, regardless of which round or level is showing. If a
   future change makes that background vary by level while the canvas stays the same pixel size
   (e.g., advancing to the next level without a resize), the old picture will keep showing.
   **Re-check:** after finishing a level and moving to the next one *without* resizing or rotating,
   confirm every background/board element that should reflect the new level actually changed
   (Mill's valley art, Getijdenpoel's sand/pool layout, Stuifzwam's board, Opgraving's land and trench,
   Market's stall/counter). If it looks identical to the previous level, and it shouldn't, that's
   this bug back.

## What is deliberately not covered here

- **Frame rate / performance.** The dev machine runs several agents at once; any number measured
  right now is noise, not signal. Performance needs its own pass on an idle machine, not a place in
  this checklist.
- **Audio content correctness** (mixes, synthesis quality) — `src/util/audio.ts` and the per-game
  `*sfx.ts` files are a sound-design concern, not a functional regression risk covered here. This
  checklist only expects that audio doesn't throw a console error.
- **Dutch-language text review** — the i18n switch (`NL()`/`T()` in each game) is exercised by these
  paths incidentally (whichever the `navigator.language` gives you), but translation accuracy and
  completeness need a bilingual reviewer, not a QA click-through.
- **Native shell behaviour** (Capacitor iOS/Android, PWA install, app icons) — this checklist is
  scoped to the web build served by Vite. Native packaging has its own release checklist once it
  exists.
- **Full playthroughs of every level/valley/mission/night.** The critical paths above prove each
  game's core loop still works, not that all eight levels of every game are individually bug-free.
  Content-level bugs within a specific level are tracked as ordinary bugs, not regression blockers.
- **Save/progress persistence across sessions** (coins, unlocked levels, `localStorage`) — worth its
  own pass, but not exercised by these single-session critical paths.
